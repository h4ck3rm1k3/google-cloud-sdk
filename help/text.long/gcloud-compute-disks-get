NAME
       gcloud-compute-disks-get - Get Google Compute Engine disks

SYNOPSIS
       gcloud compute disks get
         [NAME_REGEX ...]
         [--fields FIELDS [FIELDS ...]]
         [--help]
         [--limit LIMIT]
         [--sort-by SORT_BY]
         [--zones ZONE [ZONE ...]]
         [-h]

DESCRIPTION
       gcloud compute disks get displays all data associated with Google
       Compute Engine disks in a project.

       By default, disks from all zones are fetched. The results can be
       narrowed down by providing "--zone".

POSITIONAL ARGUMENTS
       [NAME_REGEX ...]
           Name regular expressions used to filter the resources fetched. The
           regular expressions must conform to the re2 syntax (see
           https://code.google.com/p/re2/wiki/Syntax).

FLAGS
       --fields FIELDS [FIELDS ...]
           Fields to display. Possible values are:

               creationTimestamp
               description
               id
               kind
               licenses[]
               name
               options
               selfLink
               sizeGb
               sourceImage
               sourceImageId
               sourceSnapshot
               sourceSnapshotId
               status
               type
               zone

       --help
           Display detailed help.

       --limit LIMIT
           The maximum number of results.

       --sort-by SORT_BY
           A field to sort by. To perform a descending-order sort, prefix the
           value of this flag with a tilde ("~").

       --zones ZONE [ZONE ...]
           If provided, only resources from the given zones are queried.

       -h
           Print a summary help and exit.

