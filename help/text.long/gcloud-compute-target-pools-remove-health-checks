NAME
       gcloud-compute-target-pools-remove-health-checks - Remove an HTTP
       health check from a target pool

SYNOPSIS
       gcloud compute target-pools remove-health-checks
         NAME
         [--help]
         --http-health-check HEALTH_CHECK
         [--region REGION]
         [-h]

DESCRIPTION
       gcloud compute target-pools remove-health-checks is used to remove an
       HTTP health check from a target pool. Health checks are used to
       determine the health status of instances in the target pool. For more
       information on health checks and load balancing, see
       https://developers.google.com/compute/docs/load-balancing/.

POSITIONAL ARGUMENTS
       NAME
           The name of the target pool from which to remove the health check.

FLAGS
       --help
           Display detailed help.

       --http-health-check HEALTH_CHECK
           Specifies an HTTP health check object to remove from the target
           pool.

       --region REGION
           The region of the target pool to remove health checks from. If
           omitted and the arguments are not all URIs, you will be prompted to
           select a region.

       -h
           Print a summary help and exit.

