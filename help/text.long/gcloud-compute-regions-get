NAME
       gcloud-compute-regions-get - Get Google Compute Engine regions

SYNOPSIS
       gcloud compute regions get
         [NAME_REGEX ...]
         [--fields FIELDS [FIELDS ...]]
         [--help]
         [--limit LIMIT]
         [--sort-by SORT_BY]
         [-h]

DESCRIPTION
       gcloud compute regions get displays all data associated with Google
       Compute Engine regions.

POSITIONAL ARGUMENTS
       [NAME_REGEX ...]
           Name regular expressions used to filter the resources fetched. The
           regular expressions must conform to the re2 syntax (see
           https://code.google.com/p/re2/wiki/Syntax).

FLAGS
       --fields FIELDS [FIELDS ...]
           Fields to display. Possible values are:

               creationTimestamp
               deprecated.deleted
               deprecated.deprecated
               deprecated.obsolete
               deprecated.replacement
               deprecated.state
               description
               id
               kind
               name
               quotas[].limit
               quotas[].metric
               quotas[].usage
               selfLink
               status
               zones[]

       --help
           Display detailed help.

       --limit LIMIT
           The maximum number of results.

       --sort-by SORT_BY
           A field to sort by. To perform a descending-order sort, prefix the
           value of this flag with a tilde ("~").

       -h
           Print a summary help and exit.

