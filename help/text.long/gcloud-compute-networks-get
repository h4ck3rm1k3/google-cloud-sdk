NAME
       gcloud-compute-networks-get - Get Google Compute Engine networks

SYNOPSIS
       gcloud compute networks get
         [NAME_REGEX ...]
         [--fields FIELDS [FIELDS ...]]
         [--help]
         [--limit LIMIT]
         [--sort-by SORT_BY]
         [-h]

DESCRIPTION
       gcloud compute networks get displays all data associated with Google
       Compute Engine networks in a project.

POSITIONAL ARGUMENTS
       [NAME_REGEX ...]
           Name regular expressions used to filter the resources fetched. The
           regular expressions must conform to the re2 syntax (see
           https://code.google.com/p/re2/wiki/Syntax).

FLAGS
       --fields FIELDS [FIELDS ...]
           Fields to display. Possible values are:

               IPv4Range
               creationTimestamp
               description
               gatewayIPv4
               id
               kind
               name
               selfLink

       --help
           Display detailed help.

       --limit LIMIT
           The maximum number of results.

       --sort-by SORT_BY
           A field to sort by. To perform a descending-order sort, prefix the
           value of this flag with a tilde ("~").

       -h
           Print a summary help and exit.

